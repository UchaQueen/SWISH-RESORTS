import React from 'react'
import b from "../component/Button.module.css"

const Button = () => {
  return (
    <div className={b.button}>
      <button>for more enquires</button>
    </div>
  )
}

export default Button
